installing this config is as simple as:

  git clone https://github.com/abrasumente233/nvim.config.git ~/.config/nvim

then start up nvim, the plugin manager will start installing the plugins automatically.

note: i install plugins from GitHub so you probably need a VPN.
